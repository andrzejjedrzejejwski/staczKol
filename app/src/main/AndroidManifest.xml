<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aj.software.staczKolejkowy" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".core.App"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="aj.software.staczKolejkowy.DepartmentsActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".core.RemoteApiService" android:exported="false" />

        <activity android:name=".StandsActivity" />
        <activity android:name=".InfoActivity" android:windowSoftInputMode="adjustPan" />

    </application>

</manifest>
